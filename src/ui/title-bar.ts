import { Foregrounds, UIColors } from '../colors.ts';

export const titleBar = {
  'titleBar.activeBackground': UIColors.Background, // Title Bar background when the window is active.
  'titleBar.activeForeground': Foregrounds.Support, // Title Bar foreground when the window is active.
  'titleBar.inactiveBackground': UIColors.Background, // Title Bar background when the window is inactive.
  'titleBar.inactiveForeground': Foregrounds.Muted, // Title Bar foreground when the window is inactive.
  'titleBar.border': UIColors.Border, // Title bar border color.
};
